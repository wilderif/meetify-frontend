import styled from "styled-components";

const ChatMsgProfileWrapper = styled.div`
  display: flex;
  align-items: flex-start;
  gap: 8px;

  img {
    width: 45px;
    height: 45px;
  }
`;

export default ChatMsgProfileWrapper;
